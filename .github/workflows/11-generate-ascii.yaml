name: Generate ASCII 
on: 
  workflow_dispatch:
    
jobs:
  ascii_pattern:
    runs-on: ubuntu-latest
    steps:
      - name: code Checkout
        uses: actions/checkout@v4
      
      - name: list files
        run: ls -ltra
        
      - name: Install cowsay
        run: | 
          chmod +x ascii-shell.sh
          ascii-shell.sh
