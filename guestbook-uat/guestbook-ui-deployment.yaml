apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui-uat
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-ui-uat
  template:
    metadata:
      labels:
        app: guestbook-ui-uat
    spec:
      containers:
        - image: gcr.io/google-samples/gb-frontend:v5
          name: guestbook-ui-uat
          ports:
            - containerPort: 80
